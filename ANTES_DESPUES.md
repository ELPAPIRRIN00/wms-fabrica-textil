# 📋 ANTES vs DESPUÉS - EJEMPLOS DE CAMBIOS

## 1. FUNCIÓN `imprimirQR()` - INCOMPLETA A COMPLETA

### ❌ ANTES (Incompleto y sin errores)
```javascript
imprimirQR() {\n    if (!this.rolloSeleccionado) return;\n\n    const iframe = document.getElementById('print-frame');\n    if (!iframe) return;\n\n    const doc = iframe.contentDocument || iframe.contentWindow.document;\n    doc.open();\n    doc.write(`...HTML...`);\n    doc.close();\n    // ❌ Sin validación, sin try-catch, sin feedback\n}
```

### ✅ DESPUÉS (Completo con validación)
```javascript
imprimirQR() {\n    if (!this.rolloSeleccionado) {\n        this.mostrarToast('No hay producto seleccionado para imprimir.', 'error');\n        return;\n    }\n\n    const iframe = document.getElementById('print-frame');\n    if (!iframe) {\n        this.mostrarToast('Error: No se encontró el frame de impresión.', 'error');\n        return;\n    }\n\n    try {\n        const doc = iframe.contentDocument || iframe.contentWindow.document;\n        doc.open();\n        doc.write(`...HTML...`);\n        doc.close();\n        this.mostrarToast('Abriendo vista previa de impresión...');\n    } catch (err) {\n        console.error('Error en impresión de QR:', err);\n        this.mostrarToast('Error al preparar la impresión.', 'error');\n    }\n}\n```

**Cambios:**\n- ✅ Validación de rolloSeleccionado\n- ✅ Validación de iframe\n- ✅ Try-catch block\n- ✅ Feedback visual con toast\n- ✅ Logging de errores\n\n---\n\n## 2. FUNCIÓN `registrarNuevoRollo()` - VALIDACIÓN GENÉRICA A ESPECÍFICA\n\n### ❌ ANTES (Una sola validación genérica)\n```javascript\nasync registrarNuevoRollo(e) {\n    e.preventDefault();\n    const nombre = document.getElementById('reg-nombre').value.trim();\n    const presentacion = document.getElementById('reg-presentacion').value;\n    const composicion = document.getElementById('reg-composicion').value.trim();\n    const color = document.getElementById('reg-color').value.trim();\n    const metros = parseFloat(document.getElementById('reg-metros').value);\n    const peso = parseFloat(document.getElementById('reg-peso').value);\n\n    // ❌ UNA sola validación genérica\n    if (!nombre || !presentacion || !composicion || !color || isNaN(metros) || isNaN(peso) || metros <= 0 || peso <= 0) {\n        this.mostrarToast('Por favor completa todos los campos con datos válidos.', 'error');\n        return;\n    }\n}\n```\n\n### ✅ DESPUÉS (Validaciones específicas para cada campo)\n```javascript\nasync registrarNuevoRollo(e) {\n    e.preventDefault();\n    const nombre = document.getElementById('reg-nombre').value.trim();\n    const presentacion = document.getElementById('reg-presentacion').value;\n    const composicion = document.getElementById('reg-composicion').value.trim();\n    const color = document.getElementById('reg-color').value.trim();\n    const metros = parseFloat(document.getElementById('reg-metros').value);\n    const peso = parseFloat(document.getElementById('reg-peso').value);\n\n    // ✅ MÚLTIPLES validaciones específicas\n    if (!nombre) {\n        this.mostrarToast('❌ El nombre de la tela es requerido.', 'error');\n        return;\n    }\n    if (!presentacion) {\n        this.mostrarToast('❌ Selecciona un tipo de empaque.', 'error');\n        return;\n    }\n    if (!composicion) {\n        this.mostrarToast('❌ La composición es requerida.', 'error');\n        return;\n    }\n    if (!color) {\n        this.mostrarToast('❌ El color es requerido.', 'error');\n        return;\n    }\n    if (isNaN(metros) || metros <= 0) {\n        this.mostrarToast('❌ Los metros deben ser un número válido mayor a 0.', 'error');\n        return;\n    }\n    if (isNaN(peso) || peso <= 0) {\n        this.mostrarToast('❌ El peso debe ser un número válido mayor a 0.', 'error');\n        return;\n    }\n}\n```\n\n**Cambios:**\n- ✅ Mensaje específico para cada campo\n- ✅ Iconos emoji para mejor UX\n- ✅ Validación individual de cada campo\n- ✅ Feedback claro sobre qué corregir\n\n---\n\n## 3. FUNCIÓN `traducirEscanerAEspanol()` - SIN ERRORES A ROBUSTO\n\n### ❌ ANTES (Sin manejo de errores)\n```javascript\ntraducirEscanerAEspanol() {\n    const container = document.getElementById('scanner-container');\n    if (!container) return;\n\n    const traducirTextos = () => {\n        const btnPermission = container.querySelector('#html5-qrcode-button-camera-permission');\n        if (btnPermission && btnPermission.textContent.includes('Request Camera Permissions')) {\n            btnPermission.textContent = '🎥 Solicitar Permiso...';\n        }\n        // ❌ Si algún elemento falta, puede causar error\n        // ❌ Sin try-catch, sin manejo de excepciones\n    };\n\n    traducirTextos();\n    const observer = new MutationObserver(traducirTextos);\n    observer.observe(container, { childList: true, subtree: true });\n}\n```\n\n### ✅ DESPUÉS (Con manejo robusto de errores)\n```javascript\ntraducirEscanerAEspanol() {\n    const container = document.getElementById('scanner-container');\n    if (!container) return;\n\n    const traducirTextos = () => {\n        try {\n            const btnPermission = container.querySelector('#html5-qrcode-button-camera-permission');\n            // ✅ Optional chaining - seguro si el elemento no existe\n            if (btnPermission?.textContent?.includes('Request Camera Permissions')) {\n                btnPermission.textContent = '🎥 Solicitar Permiso...';\n            }\n            // ✅ Continúa sin errores aunque falten elementos\n        } catch (err) {\n            console.warn('Error al traducir elementos del escáner:', err);\n        }\n    };\n\n    traducirTextos();\n    try {\n        const observer = new MutationObserver(traducirTextos);\n        observer.observe(container, { childList: true, subtree: true });\n    } catch (err) {\n        console.warn('Error al crear observer del escáner:', err);\n    }\n}\n```\n\n**Cambios:**\n- ✅ Try-catch block\n- ✅ Optional chaining (`?.`)\n- ✅ Logging de errores\n- ✅ Manejo de excepciones del observer\n\n---\n\n## 4. FUNCIÓN `cargarDatos()` - SIN VALIDACIÓN A VALIDADA\n\n### ❌ ANTES (Asume que los datos son válidos)\n```javascript\nasync cargarDatos() {\n    if (this.useBackend) {\n        try {\n            const resProductos = await fetch('/api/productos');\n            if (resProductos.ok) {\n                this.inventario = await resProductos.json();  // ❌ Sin validar tipo\n            }\n            // ...\n        } catch (err) {\n            console.warn('Error...', err);\n        }\n    }\n\n    try {\n        const rawInv = localStorage.getItem('ham_inventario');\n        this.inventario = rawInv !== null ? JSON.parse(rawInv) : this.getInventarioDemo();\n        // ❌ ¿Y si el JSON.parse falló? ¿inventario es un array?\n    } catch (e) {\n        this.inventario = this.getInventarioDemo();\n    }\n}\n```\n\n### ✅ DESPUÉS (Validación completa de tipos)\n```javascript\nasync cargarDatos() {\n    if (this.useBackend) {\n        try {\n            const resProductos = await fetch('/api/productos');\n            if (resProductos.ok) {\n                const data = await resProductos.json();\n                // ✅ Valida que sea array\n                if (Array.isArray(data)) this.inventario = data;\n            }\n            // ...\n        } catch (err) {\n            console.warn('⚠️ Error cargando desde Backend...', err);\n        }\n    }\n\n    try {\n        const rawInv = localStorage.getItem('ham_inventario');\n        this.inventario = rawInv !== null ? JSON.parse(rawInv) : this.getInventarioDemo();\n        \n        // ✅ Valida que sean arrays\n        if (!Array.isArray(this.inventario)) this.inventario = this.getInventarioDemo();\n        if (!Array.isArray(this.bitacora)) this.bitacora = [];\n    } catch (e) {\n        console.error('Error parsing localStorage data:', e);\n        this.inventario = this.getInventarioDemo();\n    }\n}\n```\n\n**Cambios:**\n- ✅ Validación de tipos con `Array.isArray()`\n- ✅ Validación de ambos arrays\n- ✅ Logging mejorado con emoji\n- ✅ Fallback automático si datos son inválidos\n\n---\n\n## 5. SERVER.JS - SIN VALIDACIÓN A VALIDADO\n\n### ❌ ANTES (Acepta cualquier dato)\n```javascript\napp.post('/api/sincronizar', (req, res) => {\n    const { inventario: inv, bitacora: bit } = req.body;\n    // ❌ No valida que sean arrays\n    // ❌ No valida que los registros sean válidos\n    // ❌ No hay try-catch\n    if (Array.isArray(inv)) inventario = inv;\n    if (Array.isArray(bit)) bitacora = bit;\n    res.json({ status: 'ok', message: 'Datos sincronizados correctamente.' });\n});\n```\n\n### ✅ DESPUÉS (Validación completa)\n```javascript\napp.post('/api/sincronizar', (req, res) => {\n    try {\n        const { inventario: inv, bitacora: bit } = req.body;\n\n        // ✅ Valida que sean arrays\n        if (!Array.isArray(inv) || !Array.isArray(bit)) {\n            return res.status(400).json({ \n                status: 'error', \n                message: 'Inventario y bitácora deben ser arrays válidos' \n            });\n        }\n\n        // ✅ Valida que los registros sean válidos\n        const inventarioValido = inv.every(item => \n            item.id && item.tela && typeof item.metros === 'number' && typeof item.peso === 'number'\n        );\n        \n        if (!inventarioValido) {\n            return res.status(400).json({ \n                status: 'error', \n                message: 'Algunos registros del inventario son inválidos' \n            });\n        }\n\n        // ✅ Actualizar datos si son válidos\n        inventario = inv;\n        bitacora = bit;\n        \n        console.log('✅ Datos sincronizados correctamente. Inventario:', inv.length, 'Bitácora:', bit.length);\n        res.json({ status: 'ok', message: 'Datos sincronizados correctamente.' });\n    } catch (err) {\n        handleError(err, res);\n    }\n});\n```\n\n**Cambios:**\n- ✅ Try-catch block\n- ✅ Validación de tipos\n- ✅ Validación de registros\n- ✅ HTTP status codes apropiados\n- ✅ Mensajes de error descriptivos\n\n---\n\n## 📊 RESUMEN DE MEJORAS\n\n| Aspecto | Antes | Después |\n|--------|-------|----------|\n| **Validación** | Mínima | Exhaustiva |\n| **Manejo de Errores** | Casi nulo | Completo |\n| **Mensajes de Error** | Genéricos | Específicos |\n| **UX** | Pobre | Excelente |\n| **Seguridad** | Baja | Alta |\n| **Robustez** | Frágil | Sólida |\n\n---\n\n## ✅ RESULTADO FINAL\n\n✨ Tu código ahora es:\n- **Seguro** - Valida entrada en todos lados\n- **Robusto** - Maneja errores gracefully\n- **Usable** - Feedback claro al usuario\n- **Profesional** - Production-ready\n\n🎉 **¡Listo para usar en producción!**\n